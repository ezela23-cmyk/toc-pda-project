Alphabet Definition
The alphabet Σ for the language is defined as:

Σ = { f, g, x, (, ) }

Explanation
f, g → function symbols
x → variable
( → opening parenthesis
) → closing parenthesis
This restricted alphabet simplifies the modeling while preserving nested structure behavior.
